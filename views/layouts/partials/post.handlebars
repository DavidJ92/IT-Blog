{{!< main}}

<h2>{{post.title}}</h2>
<p>{{post.content}}</p>
<p>Created by: {{post.User.username}} on {{post.createdAt}}</p>

<h3>Comments</h3>
{{#if comments}}
  <ul>
    {{#each comments}}
      <li>{{this.text}} - {{this.User.username}} on {{this.createdAt}}</li>
    {{/each}}
  </ul>
{{else}}
  <p>No comments yet.</p>
{{/if}}

<!-- Comment form here -->

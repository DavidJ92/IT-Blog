const router = require('express').Router();
const { Post } = require('../../models');

// Define post routes (get all posts, get single post, create post, update post, delete post)

module.exports = router;

const router = require('express').Router();
const { Comment } = require('../../models');

// Define comment routes (get all comments, get comments by post, create comment, update comment, delete comment)

module.exports = router;
